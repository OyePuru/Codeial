    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" >Codeial</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <% if(!locals.user) { %>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/">Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/users/connect">Connect People</a>
              </li>
            </ul>
            <form class="d-flex">
              <button class="btn btn-outline-success me-2" onclick="location.href='/users/sign-in'" type="button">Sign-In</button>
              <button class="btn btn-outline-success me-2" onclick="location.href='/users/sign-up'" type="button">Sign-Up</button>
            </form>
          </div>
          <% } %>
          <% if(locals.user) { %>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Home</a>
                  </li>
                  <a class="nav-link active" aria-current="/users/profile" href="/users/profile/<%= user.id %>"><%= user.name %></a>
                  <li class="nav-item">
                  </li>
                  <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/users/connect">Explore</a>
                  </li>
                </ul>
                <form class="d-flex">
                  <button class="btn btn-outline-success me-2" onclick="location.href='/users/sign-out'" type="button">Sign-Out</button>
                </form>
              </div>
          <% } %>
        </div>
      </nav>